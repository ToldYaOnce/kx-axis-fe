import React from 'react';
import ReactDOM from 'react-dom/client';
import { DemoAppRouted } from './DemoAppRouted';

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <DemoAppRouted />
  </React.StrictMode>
);
